name: Production Deployment

on:
  push:
    branches:
      - main

jobs:
  deployment:
    runs-on: ubuntu-latest

    environment:
      name: production
      url: https://loanrisk-ai.streamlit.app

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Mark deployment successful
        run: echo "Deployment successful"